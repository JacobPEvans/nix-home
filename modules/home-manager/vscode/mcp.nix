# VS Code MCP Server Configuration
#
# MCP (Model Context Protocol) server definitions for VS Code.
# These are merged into ~/Library/Application Support/Code/User/mcp.json
# via activation script (writable at runtime).
#
# Add MCP server entries here as needed. Format matches VS Code's mcp.json schema.

{ }
